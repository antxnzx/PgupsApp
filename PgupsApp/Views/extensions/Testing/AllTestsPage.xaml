<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PgupsApp.Views.extensions.Testing.AllTestsPage"
             xmlns:viewModels="clr-namespace:PgupsApp.ViewModels.extensions.Testing"
             Title="AllTestsPage">
    <ContentPage.BindingContext>
        <viewModels:AllTestsPageViewModel/>
    </ContentPage.BindingContext>
    <VerticalStackLayout>
        <CollectionView x:Name="questionList" ItemsSource="{Binding AllTests}">
            <CollectionView.ItemTemplate>
                <DataTemplate>
                    <StackLayout Orientation="Vertical">

                        <Label Text="{Binding Test.Id}" />
                        <Label Text="{Binding Test.Name}" />
                    </StackLayout>
                </DataTemplate>
            </CollectionView.ItemTemplate>
        </CollectionView>
    </VerticalStackLayout>
</ContentPage>