<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="PgupsApp.Views.extensions.Testing.ResultTestMikhelsonaPage"
            xmlns:viewModels="clr-namespace:PgupsApp.ViewModels.extensions.Testing"
             Title="Результат теста"
             Shell.BackgroundColor="{StaticResource MainColor}"
             BackgroundColor="{StaticResource BackgroundColor}"
             Shell.TabBarIsVisible="False"
             NavigatedTo="ContentPage_NavigatedTo"
            >
    <Shell.BackButtonBehavior>
        <BackButtonBehavior IsEnabled="False" IsVisible="False"/>
    </Shell.BackButtonBehavior>
    <ContentPage.BindingContext>
        <viewModels:ResultTestMikhelsonaViewModel/>
    </ContentPage.BindingContext>
    <Grid RowSpacing="7">
        <Grid.RowDefinitions>
            <RowDefinition Height="0"/>
            <RowDefinition Height="12*"/>
            <RowDefinition Height="12*"/>
            <RowDefinition Height="12*"/>
            <RowDefinition Height="44*"/>
            <RowDefinition Height="14*"/>
            <RowDefinition Height="6*"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="50*"/>
            <ColumnDefinition Width="25*"/>
            <ColumnDefinition Width="25*"/>
        </Grid.ColumnDefinitions>
        <Frame Grid.ColumnSpan="3" CornerRadius="15" Grid.Row="1" Grid.Column="0" BackgroundColor="{StaticResource ForegroundColor}"/>
        <Frame Grid.ColumnSpan="3" CornerRadius="15" Grid.Row="2" Grid.Column="0" BackgroundColor="{StaticResource ForegroundColor}"/>
        <Frame Grid.ColumnSpan="3" CornerRadius="15" Grid.Row="3" Grid.Column="0" BackgroundColor="{StaticResource ForegroundColor}"/>
        <Frame Grid.ColumnSpan="3" Grid.RowSpan="2" CornerRadius="15" Grid.Row="4" Grid.Column="0" BackgroundColor="{StaticResource ForegroundColor}"/>

        <Label Style="{StaticResource AllText}" Grid.Row="1" Grid.Column="0" Text="Зависимая позиция в общении (неуверенное поведение)"/>
        <Label Style="{StaticResource AllText}" Grid.Row="1" Grid.Column="1" Text="{Binding PercentZavis}"/>
        <Label Style="{StaticResource AllText}" Grid.Row="1" Grid.Column="2" Text="{Binding GradeZavis}"/>


        <Label Style="{StaticResource AllText}" Grid.Row="2" Grid.Column="0" Text="Компетентная позиция в общении (уверенное поведение)" />
        <Label Style="{StaticResource AllText}" Grid.Row="2" Grid.Column="1" Text="{Binding PercentCompetent}"/>
        <Label Style="{StaticResource AllText}" Grid.Row="2" Grid.Column="2" Text="{Binding GradeCompetent}"/>

        <Label Style="{StaticResource AllText}" Grid.Row="3" Grid.Column="0" Text="Агрессивная позиция в общении (давящее поведение)" />
        <Label Style="{StaticResource AllText}" Grid.Row="3" Grid.Column="1" Text="{Binding PercentAgressive}"/>
        <Label Style="{StaticResource AllText}" Grid.Row="3" Grid.Column="2" Text="{Binding GradeAgressive}"/>

        <VerticalStackLayout Margin="10" Grid.ColumnSpan="3" Grid.RowSpan="2" Grid.Row="4" Grid.Column="0">
            <Label Style="{StaticResource AllText}" Margin="0"  
               Text="Коммуникативная компетентность - это умение выйти из любой ситуации, не потеряв ни грамма собственной внутренней свободы и в то же время не дав потерять её партнеру по общению. 
               Исходя из этого определения, можно говорить о трех позициях в общении: зависимой, компетентной и агрессивной."/>
            <Label Style="{StaticResource AllText}" Margin="0,5,0,0" Text="В первом случае (зависимая позиция) наблюдается неуверенное поведение, при котором человек теряет внутреннюю свободу;"/>
            <Label Style="{StaticResource AllText}" Margin="0,5,0,0" Text="В случае с агрессивной позицией – человек «подавляет» партнера по общению, при этом партнер теряет внутреннюю свободу;"/>
            <Label Style="{StaticResource AllText}" Margin="0,5,0,0" Text="Компетентная позиция – самая оптимальная, это общение на равных, это и будет та самая коммуникативная компетентность."/>
        </VerticalStackLayout>
        
        <Button Grid.ColumnSpan="3" Grid.Row="5" Grid.Column="0"  
                HeightRequest="40" WidthRequest="200"
                VerticalOptions="Center"
                Text="Выйти из теста"
                Style="{StaticResource StandartButtons}"
                Command="{Binding ExitFromTestCommand}"/>
    </Grid>
</ContentPage>